Featuring:

- HTML parsing with html5ever or html5gum: How these libraries approach the same problem in different ways. Both are
  production grade and quite low level. html5ever is used in a web browser.
- How to structure a project with multiple files.
- Trait objects.
- A thread pool without an external crate.
- Channel (similar to Go's).
- Async, and compare async vs OS threads for an I/O heavy workload.
- CLI arguments parsing with clap.
