Featuring GraphQL: How we can use Rust compiler to enforce type safety when working with GraphQL

How to test:

- https://github.com/settings/personal-access-tokens
- export GITHUB_TOKEN=abcdef
- cargo run
